import { infoStyles } from "../../utils/styles"

export default function Description() {
  return <div className={`mb-4 ${infoStyles}`}>
    <p className="mb-4">Description</p>
    <div>A fiery young man clashes with an unflinching forest officer in a south Indian village where spirituality, fate and folklore rule the lands.</div>
  </div>
}